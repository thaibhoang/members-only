<%= form_with model: [@post, @post.comments.build] do |form| %>
  <p>
    <%= form.label :body %><br>
    <%= form.text_area :body %>
    <% if user_signed_in? %>
      <%= form.hidden_field :user_id, value: current_user.id %>
    <% end %>
  </p>
  <p>
    <%= form.submit %>
  </p>
<% end %>